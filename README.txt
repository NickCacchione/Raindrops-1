**Algorithm for Raindrop Game**

-Create a variable to keep the score.
-Create an array of raindrops from a Raindrop Class.
-Create a cather from a Catcher Class.
-Make the catcher move with the mouse.
-If the raindrop touches the cathcher, increase the score and make the raindrop start over from the top.
-If the raindrop does not touch the catcer, make te raindrop start over from the top, but do not increase the score.
